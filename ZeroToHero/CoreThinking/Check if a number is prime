import math
count = 0
num = int(input())
for i in range(1,int(math.sqrt(num))+1):
    if num % i == 0:
        count += 1
        if num // i != i:
            count += 1
if count == 2:
    print("Prime")
else:
    print("Not Prime")

another one:

import math
num = int(input())
if num <= 1:
    print("Not Prime")
else:
    for i in range(2, int(math.sqrt(num)) + 1):
        if num % i == 0:
            print("Not Prime")
            break
    else:
        print("Prime")
